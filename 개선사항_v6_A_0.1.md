# v6_A_0.1 변경 내용과 다음 개선 방향

첨부된 V6.0.22 소스를 기반으로 기존 Streamlit 메뉴에 Trade Lab을 추가했습니다. 원본 ZIP은 변경하지 않았습니다. 공유 대화는 요구사항을 이해하기 위한 참고자료로 사용했습니다.

## 사용 방법

기존처럼 requirements.txt를 설치하고 `streamlit run app.py`로 실행합니다. 개별 분석 메뉴에서 **🧮 Trade Lab**을 선택하면 종목 검색 없이 사용할 수 있습니다. 입력 금액은 모두 선택한 동일 통화 기준입니다. 현재가를 자동 조회하거나 실제 주문을 전송하지 않습니다.

1. 보유수량, 비용 포함 평균단가, 현재가, 사용 가능한 현금을 입력합니다.
2. 매매 계산 탭에서 가격과 수량을 순서대로 입력하고 계산합니다.
3. 시나리오 탭에서 조건가격과 수량을 정한 뒤 비교합니다.
4. 목표 탭에서 목표 평단, 목표수량, 일부 매도 후 재매수 수량을 확인합니다.

## 추가한 기능

- 분할 매수·매도, 물타기·불타기의 순차 계산과 거래별 현금·평단·실현손익·평가손익.
- 지속 상승, 상승 후 눌림, 박스권, 급락, V자 회복 및 직접 입력 가격 경로.
- 단순보유, 매도대금 보관, 매도대금 재사용 전략을 동일 초기 자산으로 비교.
- 매도하지 않을 핵심 보유수량 설정, 현금 부족·보유수량 초과 주문 거부.
- 목표 평단의 최소 정수 매수수량, 필요자금과 현금 부족분, 목표 보유수량.
- 매도대금만으로 재매수할 때 늘릴 수 있는 수량과 잔액.
- 보유 현금의 0/25/50/75/100%를 사용했을 때 평단·평가손익·노출·본전가 비교.
- 시나리오별 자산 경로 차트와 비교 CSV 다운로드.

## 계산 원칙

매수단가에는 매수 수수료와 불리한 체결 오차를 포함합니다. 매도 순대금에서는 매도 수수료·매도대금 기준 거래세·체결 오차를 차감합니다. 양도소득세를 계산하는 세무 도구는 아닙니다.

- 매수 후 평단 = (기존 잔여원가 + 새 매수 총비용) / 새 보유수량.
- 매도 실현손익 = 매도 순대금 − 매도수량에 해당하는 기존 평균원가. 부분 매도로 잔여 평단을 낮추지 않습니다.
- 총자산 = 현금 + 잔여수량 × 평가가격. 이 값에 실현손익을 다시 더하지 않습니다.
- 기간수익률 = 최종 총자산 / 시작 시가평가 총자산 − 1. 과거 매입가격 이후 수익률과 구분합니다.
- 잔여주식 본전가 = 잔여 평균원가 / 매도 순수취 비율.
- 전체투자 본전가 = max(0, (초기 보유원가 + 초기현금 − 현재현금) / (잔여수량 × 매도 순수취 비율)). 전량 매도 시 본전가는 표시하지 않고 청산손익을 봅니다.
- 최대낙폭 = 각 관측 총자산 / 그때까지의 최고 총자산 − 1 중 최솟값.

예: 20주, 평단 100, 현재가 70에서 1,400으로 20주를 더 사면 평단은 85입니다. 비용이 0이면 평가손실 금액은 여전히 600입니다. 본전까지 필요한 상승률은 줄지만 종목에 노출된 금액은 늘어납니다.

규칙은 각 관측점에서 처음 만족할 때 한 번만 처리됩니다. 최초 시작점에서는 거래하지 않습니다. 여러 조건이 동시에 충족되면 표 순서로 처리합니다. 가격이 조건을 건너뛰면 관측가격에 체결 오차를 반영합니다. 미체결 규칙은 자동 재시도하지 않습니다. 매수 규칙은 선행 매도를 필수 조건으로 삼지 않습니다. 정확한 매도→재매수 순서는 첫 번째 계산 탭에서 지정할 수 있습니다.

## 기존 계산에서 수정한 문제

1. **RSI 경계값**: 하락이 한 번도 없는 상승 구간에서 0으로 나누는 처리를 하면서 RSI가 결측값이 되었습니다. 상승만 있으면 100, 움직임이 없으면 50으로 처리합니다. 하락만 있으면 기존처럼 0입니다.
2. **MDD20 정의**: 기존에는 기준 종가 대비 미래 최저 종가 수익률을 계산했습니다. 이제 기준 종가부터 20거래일 종가 경로의 고점 대비 낙폭을 계산합니다. 예를 들어 100→120→110이면 기존 방식은 양수였지만 새 MDD는 −8.33%입니다. 검증 캐시 버전을 v615로 올렸으므로 다시 실행해야 합니다.
3. **검증 결과 연결**: 검증 화면은 v614 키로 저장하지만 통합 화면은 v612 이하 키만 읽던 불일치를 고쳤습니다. 저장·조회가 공통 버전 상수를 사용합니다.
4. **Windows 이력 파일 잠금**: SQLite 트랜잭션이 끝나도 연결이 닫히지 않던 부분을 수정했습니다. 정상·오류 경로 모두 연결을 종료합니다.

## 계산 방식을 더 강화할 우선순위

### 1. 실제 거래 성과 검증과 현재의 신호 검증 구분

현재 Calibration은 신호 당일 종가와 이후 종가를 비교하며, 역사적 시장 점수를 50으로 고정합니다. 따라서 실제 분할매매 전략의 거래 성과나 현재 시장 국면까지 포함한 전략의 완전한 백테스트는 아닙니다. 다음 버전에는 당일 종가로 신호를 확정하고 다음 거래일 시가부터 체결하는 검증, 갭·거래비용·손절·익절·현금 제약을 넣는 것이 우선입니다. 동일 일자에 손절·익절이 모두 닿았을 때의 처리도 명시해야 합니다.

### 2. 점수·데이터 충실도·검증 신뢰도 분리

현재 신호 일치도와 Data Confidence는 이미 구분되어 있지만, 추세와 Setup의 데이터 품질은 고정값 1.0으로 들어갑니다. 실제 결측 비율, 최신 거래일, 거래량 제공 여부, 비교지수 가용 여부를 별도로 표시하는 편이 좋습니다. 검증 신뢰도는 사례 수·독립 구간 수·성과의 불확실성으로 표시해야 합니다. 높은 분석점수를 상승확률로 해석하면 안 됩니다.

### 3. 중복 지표가 점수를 과도하게 밀어올리는지 검증

현재 Trend에는 여러 이동평균 관계와 기간 수익률이 함께 들어가고, Momentum에도 기간 수익률이 들어갑니다. Entry는 다시 Trend와 Momentum을 반영합니다. 상관된 정보를 반복 반영할 가능성이 있으므로 추세·모멘텀·거래량·상대강도·가격위치·시장환경 단위로 기여도를 표시하고, 특정 요소를 제외했을 때의 검증 결과를 비교해야 합니다. 이번 버전에서는 근거 없이 기존 가중치를 바꾸지 않았습니다.

### 4. 시간 순서 검증과 기준전략

가중치를 정한 구간과 평가 구간을 시간순으로 분리하고, 이후 구간으로 이동하며 반복 검증하는 방식이 적합합니다. 평균 수익률 외에 중앙값, 손실 꼬리, 비용 후 수익, 기준지수 대비 초과수익, 구간별 안정성을 함께 봐야 합니다. 여러 조합을 시험한 뒤 가장 좋은 결과만 선택하는 편향도 관리해야 합니다.

### 5. 수량 늘리기 전략의 검증

목표수량 달성률과 총자산 초과성과를 별도로 평가하고, 상승이 계속되어 재매수하지 못하는 경로와 급락하여 현금이 소진되는 경로를 포함해야 합니다. 다음 단계의 과거 주가 검증 후에 무작위 경로·확률 기반 시뮬레이션을 추가하는 편이 타당합니다.

## 검증 범위와 한계

- 회계·비용·현금 제약·매도금 재사용·목표 역산·RSI·MDD 등 자동 테스트 10개 통과.
- 기존 오프라인 엔진 smoke test 통과.
- Streamlit AppTest로 계산 버튼, 5개 시나리오 × 3개 전략 결과, 0주·0자산 입력, 본체 Trade Lab 메뉴 연결 확인.
- 외부 실시간 시세 조회, 실제 과거 주가에 대한 주문 체결 백테스트, 브라우저의 시각적 배치 검증은 이번 검증 범위에 포함하지 않았습니다.
- 정수 주식만 지원하며 중간 입출금, 환율, 배당, 주식분할, 차입·공매도, 확률 예측·자동 최적화는 포함하지 않습니다.
